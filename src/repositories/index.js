const Restaurants = require("./restaurant-repo");
const POCS=require('./pocs-repo');
const Orders=require('./orders-repo');
const Interactions=require('./interactions-repo');

module.exports={
    Restaurants,
    POCS,
    Orders,
    Interactions
};
